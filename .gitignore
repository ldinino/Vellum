# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Third-party styling references (fetched locally, not redistributable)
# Run scripts/fetch-references.ps1 to obtain them
vendor/reference/

# Bundled runtime binaries (large; fetched via scripts/fetch-binaries.ps1)
vendor/bin/

# Full Fugue icon set (~27 MB source material). The curated subset we ship
# lives in src/assets/icons/ and IS committed; copy more from here as needed.
/assets/

# Local dev-run artifacts (scripts/dev-run.ps1 log + machine-specific launcher)
.devlog.txt
*.lnk

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
