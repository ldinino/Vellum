/**
 * Font families + sizes offered in the editor toolbar and Settings → Editor.
 * System fonts only (spec Section 6); shared here so both surfaces stay in sync.
 * Sizes are px values (the toolbar applies them as `<n>px`, matching the
 * `--text-size-editor` base token).
 */

export const FONTS = [
  "Segoe UI",
  "Calibri",
  "Cambria",
  "Georgia",
  "Times New Roman",
  "Arial",
  "Verdana",
  "Consolas",
  "Comic Sans MS",
];

export const SIZES = ["10", "11", "12", "14", "16", "18", "24", "36"];
